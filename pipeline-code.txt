pipeline{
    agent any
    tools {
      maven 'maven3'
    }
    stages{
        stage("Git Checkout"){
            steps{
                git credentialsId: 'javahometech', url: 'https://github.com/javahometech/my-app'
            }
        }
        
        stage("Maven Build"){
            steps{
                script{
                    if(isUnix()){
                       sh 'mvn clean package' 
                    }
                }
            }
        }
    }
}
